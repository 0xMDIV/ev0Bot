// /quote [%SEARCH%] [<author>] - Gets a quote
// /addquote <quote> ~~ <author> - Adds a quote to the database (no quotation marks)
// /quotecount - Gives you the count of quotes in the database.

class Quotes {
    static quote(ctx) {
        let args = ctx.message.substring(5);

    }
}